"""
Пакет тестов. При импорте (запуск как пакет `tests.X`) подготавливает окружение
через `_bootstrap`. При запуске отдельных модулей из `tests/` ту же подготовку
выполняет пролог в каждом тест-модуле. См. `_bootstrap.py`.
"""

from . import _bootstrap
